# Plan: Step Card + Form Tanpa Card Wrapper

## Perubahan Yang Dibuat

### Revisi Struktur:

1. **Step indicator** → dibungkus card (agar step terlihat sebagai satu kesatuan)
2. **Form sections** (Data Perusahaan, PIC, dll) → TIDAK dibungkus card lagi, hanya section title dengan border-bottom

## File yang Berubah

### 1. `app/(vendor)/vendor/register/page.tsx`

- Bungkus step indicator dengan card:

```tsx
<div className="mb-6 overflow-hidden rounded-xl border border-border bg-card text-card-foreground shadow-sm">
  {/* step indicator content */}
</div>
```

### 2. `components/vendor/register/company-info-form.tsx`

- Hapus card wrapper dari setiap section
- Ganti dengan section title + border-bottom:

```tsx
<div className="space-y-4">
  <div>
    <h3 className="border-b pb-2 font-semibold text-gray-700">
      Data Perusahaan
    </h3>
    <p className="mt-1 text-xs text-muted-foreground">
      Nama Perusahaan dan Email
    </p>
  </div>
  {/* fields without card wrapper */}
</div>
```

### 3. `components/vendor/register/legal-documents-form.tsx`

- Setiap document type (KTP, NPWP, NIB, dll) tetap sebagai card karena punya header + content yang distinct

### 4. `components/vendor/register/operational-form.tsx`

- Hapus card wrapper dari setiap section
- Ganti dengan section title + border-bottom
