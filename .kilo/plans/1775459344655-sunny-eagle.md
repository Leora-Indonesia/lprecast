# Plan: Tambahkan Informasi Vendor di Landing Page

## Tujuan

Menambahkan informasi vendor registration di landing page untuk menarik vendor mendaftar.

## Perubahan yang Akan Dilakukan

### 1. Header - Tambah Navigasi "Jadi Vendor"

- Tambahkan link "Jadi Vendor" di navigation menu (sebelum atau sesudah "Tentang Kami")
- Link ini akan mengarah ke halaman registrasi vendor (jika sudah ada) atau halaman login vendor

### 2. CTA Section - Tambah Opsi "Tertarik Jadi Vendor?"

- Di section `#cta` (Hubungi Kami), tambahkan teks/konten yang mengarahkan vendor untuk mendaftar
- Misalnya: "Atau tertarik menjadi mitra vendor kami?"
- Tambahkan tombol sekunder untuk vendor registration

### 3. Hero Section (Opsional)

- Tambahkan opsi CTA untuk vendor di Hero section (misalnya "Jadi Vendor" button)

## File yang Akan Dimodifikasi

- `/Users/alvinstuff/lprecast/app/page.tsx`

## Detail Implementasi

### Header Changes (line 27-55)

```tsx
<nav className="hidden items-center gap-6 md:flex">
  <Link href="#features" className="...">
    Fitur
  </Link>
  <Link href="#how-it-works" className="...">
    Cara Kerja
  </Link>
  <Link href="#cta" className="...">
    Kontak
  </Link>
  <Link href="/about" className="...">
    Tentang Kami
  </Link>
  {/* TAMBAHAN */}
  <Link
    href="/vendor/register"
    className="text-sm font-medium text-primary transition-colors hover:text-primary/80"
  >
    Jadi Vendor
  </Link>
  <Link href="/login">
    <Button>Login</Button>
  </Link>
</nav>
```

### CTA Section Changes (line 273-280)

```tsx
<div className="mt-10 flex flex-col gap-4 sm:flex-row sm:justify-center">
  <Button asChild variant="secondary" size="lg">
    <Link href="/login">
      Hubungi Kami
      <ArrowRight className="ml-2 h-4 w-4" />
    </Link>
  </Button>
  {/* TAMBAHAN */}
  <Button
    asChild
    variant="outline"
    size="lg"
    className="border-primary text-primary hover:bg-primary/10"
  >
    <Link href="/vendor/register">
      Tertarik Jadi Vendor?
      <ArrowRight className="ml-2 h-4 w-4" />
    </Link>
  </Button>
</div>
```

## Catatan

- Perlu dibuat halaman `/vendor/register` jika belum ada
- Saat ini hanya ada `/login` dan `/vendor/dashboard`, `/vendor/profile`, `/vendor/tenders`, `/vendor/projects`

## Estimasi Perubahan

- Tambah ~5-8 baris kode di header
- Tambah ~3-5 baris di CTA section
