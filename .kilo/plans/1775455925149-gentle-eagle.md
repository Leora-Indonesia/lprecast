# Plan: PWA Automated Test Script (Bash)

## Context

- User wants to save a PWA automated test script at `scripts/test-pwa.sh`
- Project uses Serwist/Next.js PWA setup with theme-config.ts as source of truth
- No test scripts exist in the `scripts/` directory

## Implementation

### Step 1: Create `scripts/test-pwa.sh`

Based on user's provided Bash script with these adaptations for this project:

| Test   | Description               | Project-Specific Adjustments                                                                                                  |
| ------ | ------------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| TEST 1 | Required files check      | `lib/theme-config.ts`, `app/manifest.ts`, `app/sw.ts`, `app/~offline/page.tsx`, `docs/architecture/pwa.md`, `public/icon.svg` |
| TEST 2 | Build                     | `pnpm build --webpack`                                                                                                        |
| TEST 3 | Generated files           | `public/sw.js`, `.next/server/app/manifest.webmanifest`                                                                       |
| TEST 4 | Service worker validation | Check for `serwist`, `__SW_MANIFEST`, `precacheEntries`, SW size > 10KB                                                       |
| TEST 5 | Manifest validation       | Start `pnpm start`, curl manifest, validate JSON fields                                                                       |
| TEST 6 | Theme config validation   | Check for `themeConfig`, `pwa:`, `colors:`                                                                                    |
| TEST 7 | TypeScript check          | `pnpm typecheck`                                                                                                              |
| TEST 8 | Lint check                | `pnpm lint`                                                                                                                   |

### Step 2: Make script executable

```bash
chmod +x scripts/test-pwa.sh
```

### Step 3: Update package.json (optional)

Add `"test:pwa": "bash scripts/test-pwa.sh"` script if user wants via pnpm

## Files

- **Create**: `scripts/test-pwa.sh`
- **Optional**: Update `package.json` with `test:pwa` script
