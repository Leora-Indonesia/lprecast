# Plan: Update Stepper Design in Vendor Registration Page

## Overview

Replace the current stepper component in `/app/(vendor)/vendor/register/page.tsx` with a new design based on the provided HTML. Convert the static HTML to dynamic React JSX using shadcn/ui components and Tailwind CSS classes. Update step labels to match the HTML design.

## Current State Analysis

- Page uses a separate `Stepper` component imported from `@/components/vendor/register/stepper`
- Current stepper has vertical layout with connecting lines, check icons for completed steps
- Steps array: "Informasi\nPerusahaan", "Dokumen\nLegal", "Operasional", "Review &\nSubmit"
- HTML design shows horizontal layout with absolute connecting line, numbered circles, different styling

## Proposed Changes

### 1. Update Steps Array

- Change step titles to match HTML: "Info Dasar", "Legalitas", "Dokumen", "Konfirmasi"
- Remove subtitles since HTML design doesn't use them
- Update the STEPS constant in page.tsx

### 2. Replace Stepper Component Usage

- Remove import of `Stepper` component
- Replace `<Stepper />` with inline JSX based on HTML structure
- Implement dynamic rendering for 4 steps with proper active/inactive/completed states

### 3. Convert HTML to Dynamic JSX

- Container: `bg-white border border-slate-200 rounded-xl shadow-sm p-8`
- Flex layout: `flex items-center justify-between relative`
- Horizontal line: `absolute top-5 left-0 w-full h-0.5 bg-slate-100 -z-0`
- Step circles: `w-10 h-10 rounded-full` with conditional classes for active/inactive
- Active step: `bg-slate-900 text-white border border-slate-900`
- Inactive step: `bg-white border-2 border-slate-200 text-slate-400`
- All steps: `ring-4 ring-white flex items-center justify-center font-semibold`
- Labels: `mt-2 text-xs text-center` with conditional font weight and color

### 4. Implement Step Interaction

- Add click handlers for navigating to previous steps
- Disable cursor for future steps (add `cursor-not-allowed opacity-50`)
- Use `onStepClick` prop (already exists)

### 5. Handle Completed Steps

- For completed steps (index < currentStep), use same styling as active or different color
- Since HTML shows only active as filled, make completed steps outlined but clickable

### 6. Update Form Container

- Wrap the stepper and form area in the new container
- Add `mt-10 pt-10 border-t border-slate-50` for form area
- Move separator and navigation buttons inside the border-t div
- Remove `rounded-b-xl` from form container since whole area is `rounded-xl`

### 7. Testing & Validation

- Run `pnpm lint` and `pnpm typecheck` to ensure code quality
- Test stepper navigation and visual states
- Verify responsive behavior

## Files to Modify

- `/app/(vendor)/vendor/register/page.tsx` - Main changes
- Potentially check form components if padding conflicts

## Questions for User

- Should completed steps have the same filled styling as active, or remain outlined?
- Confirm the mapping of steps: Info Dasar = Company Info, Legalitas = Legal Docs, Dokumen = Operational, Konfirmasi = Review?
- Any specific behavior for completed vs active steps?
