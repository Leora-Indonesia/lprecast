export const metadata = {
  title: "Setel Password | LPrecast",
  description: "Setel password untuk akun vendor Anda",
}

export default function SetPasswordLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
