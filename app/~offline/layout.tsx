export const metadata = {
  title: "Offline | LPrecast",
  description: "Anda sedang offline, periksa koneksi internet",
}

export default function OfflineLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
