export const metadata = {
  title: "Pendaftaran Vendor | LPrecast",
  description: "Daftar sebagai vendor mitra LPrecast",
}

export default function RegisterLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
