export const metadata = {
  title: "Review Data Pendaftaran | LPrecast",
  description: "Review data pendaftaran vendor sebelum disubmit",
}

export default function ReviewLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
